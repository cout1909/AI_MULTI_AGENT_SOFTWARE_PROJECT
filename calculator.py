"""Calculator module providing arithmetic operations."""


def add(a: float | int, b: float | int) -> float | int:
    """Return the sum of two numeric arguments.

    Args:
        a: First numeric argument (int or float).
        b: Second numeric argument (int or float).

    Returns:
        The sum of a and b.
    """
    return a + b
